{% extends "base.html.twig" %}

{% block body %}
                {% if mes_erreurs is not empty %}
            <div class="alert alert-danger">
                {{ mes_erreurs|join('<br>')|raw }} 
            </div>
        {# {% elseif mes_validation is not empty %}    
            <div class="alert alert-success">
                {{ mes_validation|join('<br>')|raw 
            }} 
            </div> #}
        {% endif %}
        <div class="container my-5">

            <h3 class="pb-2 text-center secondary-title">C<span class="gold-ookan">o</span>nnectez-v<span class="gold-ookan">o</span>us</h3>

            <div class="row justify-content-center">

                <form class="bg-light border p-4 col-lg-4 col-md-6 col-10 montserrat" method="post">

                    <div class="form-group">
                        <input type="email" class="form-control" name="email" id="email" placeholder="Email">
                    </div>

                    <div class="form-group">
                        <input type="password" class="form-control" name="password" id="password" placeholder="Mot de passe">
                    </div>

                    <div class="form-group form-check">
                        <input type="checkbox" class="form-check-input" name="check-connexion" id="check-connexion">
                        <label class="w-100 form-check-label" for="exampleCheck1">Rester connecter</label>
                    </div>

                    <div class="form-group">
                        <a name="forget" href="{{ path('password_forget') }}">Mot de passe oublier ?</a>
                    </div>
                    
                <button type="submit" class="btn text-center grey-ookan">Connexi<scan class="gold-ookan">Oo</scan>n</button>                    
                </form>
            </div>
        </div>
    {% endblock %}
